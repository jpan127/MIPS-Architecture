# Currently the output is correct somehow??
# Need to replace register names with numbers
main:
    addi $a0, $0, 4
    jal  factorial
    nop
    nop
    add $s0, $v0, $0
    j end

factorial:
    addi $sp, $sp, -8
    sw $a0, 4($sp) # This needs forward from <result> but is not getting it which messes everything up
    sw $ra, 0($sp)
    addi $t0, $0, 2
    slt $t0, $a0, $t0
    beq $t0, $0, else
    nop
    nop
    addi $v0, $0, 1
    addi $sp, $sp, 8
    jr $ra
    nop
    nop

else:
    addi $a0, $a0, -1
    jal factorial
    nop
    nop
    lw $ra, 0($sp)
    lw $a0, 4($sp)
    addi $sp, $sp, 8
    multu $a0, $v0
    mflo $v0
    jr $ra
    nop
    nop

end:
    j end